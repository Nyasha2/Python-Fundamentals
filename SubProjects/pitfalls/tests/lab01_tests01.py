import sys
import pytest

sys.path.insert(0, '../lab01_pitfalls')

from lab01 import welcome_menu, init_numbers, is_game_over


def test_welcome_menu_compiles():
    assert(True)

def test_init_numbers_compiles():
    assert(True)

def test_is_game_over_compiles():
    assert(True)